
{# Menu links (sidebar), Title (header), teaser (subtitle), info (body) #}

<aside class="large-4 columns" style="border-left-style: solid; border-right-style: solid; border-width: 2px; border-color: white;">
	This is the sidebar

	{% for mod in config.get('menu/mc-mods') %}
		{{ dunp(mod['label']) }}
		{% if mod['label'] == home.title %}
			Found slug
		{% endif %}
	{% endfor %}


</aside>
